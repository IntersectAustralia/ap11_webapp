- content_for :breadcrumb do
  -title "Experiments / #{@experiment.title}"

%table.table
  %tr
    %th Title
    %th.table-value
      = @experiment.title
  %tr
    %th Date
    %th.table-value
      = @experiment.date
  %tr
    %th Owner
    %th.table-value
      = @experiment.user.full_name
  %tr
    %th Input Collections
    %th.table-value
      - @input_collections.each do |input|
        %div= link_to input.name, experiment_input_collection_path(@experiment, input)
  %tr
    %th Output Collection
    %th.table-value
      None

.form-actions
  %a.btn.btn-primary{:href => edit_experiment_path(@experiment)} Edit Experiment
  %a.btn{:href => experiments_path} Back
