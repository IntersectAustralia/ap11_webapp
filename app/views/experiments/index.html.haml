%h1 Listing experiments

%table{:class => "table table-striped"}
  %thead
    %tr
      %th Title
      %th Date
      %th Owner
      %th
      %th
      %th

  - @experiments.each do |experiment|
    %tr
      %td= experiment.title
      %td= experiment.date
      %td= experiment.user.full_name
      %td= link_to 'Show', experiment
      %td= link_to 'Edit', edit_experiment_path(experiment)
      %td
        %a{"data-toggle" => "modal", :href => "#myModal#{experiment.id}"} Delete
        .modal.hide{:id => "myModal#{experiment.id}"}
          .modal-header
            %button.close{"data-dismiss" => "modal", :type => "button"} ×
            %h3 #{experiment.title}
          .modal-body
            %i.icon-exclamation-sign.icon-black
            Are you sure you want to delete experiment #{experiment.title}?
          .modal-footer
            %a.btn{"data-dismiss" => "modal", :href => '#'} Close
            %a.btn.btn-danger{:href => '/experiments/' + experiment.id.to_s, "data-method" => "delete"} Delete
%br

%a{:class => "btn btn-primary", :href => new_experiment_path}
  %i{:class => "icon-file icon-white"}
  New Experiment