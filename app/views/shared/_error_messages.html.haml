- if target.errors.any?
  .alert.alert-error
    %a.close{"data-dismiss" => "alert", :href => "#"} ×
    %h3.error_header The record could not be saved
    %p.notification{:id => "*.errors"}
      - target.errors.full_messages.each do |msg|
        %div
          = msg
